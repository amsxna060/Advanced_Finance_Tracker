# Package-by-feature modules (E4+). Each subpackage owns its models,
# schemas, service and router, and exposes a narrow public interface via
# its service module — other code must not reach into its internals.
