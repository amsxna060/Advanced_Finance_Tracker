# Render.com deployment configuration (Blueprint v2)
# This file is optional - you can also configure services manually via the Render dashboard
# For manual setup instructions, see DEPLOY_RENDER.md

# Note: This blueprint format may need adjustment based on Render's current API
# It's recommended to set up services manually via the dashboard instead